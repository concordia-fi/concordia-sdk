{
  "name": "examples",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "ping": "tsc --build && ts-node src/ping.ts",
    "init-profile": "tsc --build && ts-node src/init-profile.ts",
    "lend": "tsc --build && ts-node src/lend.ts"
  },
  "dependencies": {
    "@types/node": "18.6.2",
    "aptos": "^1.3.17",
    "commander": "^9.0.0",
    "typescript": "4.8.2"
  },
  "devDependencies": {
    "ts-node": "10.9.1"
  }
}
